/*const mongoose = require('mongoose');
const userSchema = require('./userSchema');

const VendorSchema = userSchema.extend({
  userType: {
    type: String,
    required: true,
    default: 'Vendor',
  },
});

module.exports = mongoose.model('Vendor', VendorSchema);*/
