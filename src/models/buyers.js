/**const mongoose = require('mongoose');
const userSchema = require('./userSchema');

const BuyerSchema = userSchema.extend({
  userType: {
    type: String,
    required: true,
    default: 'Buyer',
  },
  // Additional methods or properties specific to Buyer
});

module.exports = mongoose.model('Buyer', BuyerSchema);*/
